# PyTorch is installed separately with CUDA wheels (NOT pinned here, or pip pulls the
# CPU build from PyPI). Must be torch >= 2.7 — transformers 5.x references
# torch.float8_e8m0fnu, which only exists in 2.7+. Use a recent CUDA index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126

# Diffusion / inpainting
diffusers>=0.30.0
transformers>=4.44.0          # provides GroundingDINO + SAM, no CUDA compilation
accelerate>=0.33.0
safetensors>=0.4.0

# Image processing
opencv-python>=4.10.0
pillow>=10.4.0
numpy>=1.26.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # file uploads

# Quantized Qwen-Image-Edit stack
peft>=0.19.0                  # required to load the Lightning LoRA
bitsandbytes>=0.49.0          # 4-bit text encoder

# Misc
requests>=2.32.0
